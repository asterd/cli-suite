# Milestone 1 Status Report

## What was implemented
- Added `axt-core` foundations: `ErrorCode`, exit-code mapping, `STANDARD_ERROR_CATALOG`, retryability markers, and serde string serialization for error codes.
- Added shared CLI primitives in `axt-core`: `OutputMode`, clap-backed output mode conflict detection, output limit flags, `--print-schema`, and `--list-errors` support through `CommonArgs`.
- Added `Clock`, `SystemClock`, and feature/test-gated `FixedClock`, plus `CommandContext`, UTF-8 current-directory handling, and color choice resolution for `NO_COLOR`, `CLICOLOR_FORCE`, and `FORCE_COLOR`.
- Added `axt-output` foundations: `Renderable`, `RenderContext`, `JsonEnvelope`, `OutputDiagnostic`, typed output errors, style helpers, and stdout/stderr stream helpers using `anstream`/`anstyle`.
- Added `JsonlWriter` and `AgentCompactWriter`, with output limits, truncation warning records, and strict truncation errors for JSONL and ACF respectively.
- Rewired the existing `axt-peek` stub to use `axt-core` and `axt-output` while preserving the existing stub data payloads for human, JSON, JSON data, JSONL, and agent modes.
- Added a tiny published schema for the M1 `axt.peek.v1` stub output at `schemas/axt.peek.v1.schema.json`.
- Added unit tests for `axt-core` and `axt-output`, plus `axt-peek` integration tests with inline snapshots, JSON schema validation, JSONL validation, ACF checks, catalog listing checks, and clap conflict checks.

## What was not implemented and why
- No real `axt-peek` filesystem, git, language, or traversal behavior was implemented because that belongs to Milestone 3.
- No `axt-fs`, `axt-git`, or later binary behavior was implemented because this session is limited to Milestone 1.
- Full cross-platform CI was not observed from this local session; only local quality gates were run.

## Deviations from the spec
- No intentional spec deviations.
- The M1 "hello world" binary was implemented by rewiring the existing `axt-peek` stub, not by adding a new binary, to respect the project rule forbidding binaries beyond the six named commands.

## How to run tests
```bash
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

## How to run the release pipeline locally
```bash
dist generate --check
dist plan --output-format=json
```

These were attempted in this session, but both failed before planning because `cargo metadata` tried to download uncached crates and the sandbox could not resolve `static.crates.io`.

## Quality gates: pass / fail
- fmt: ✅
- clippy: ✅
- test: ✅
- CI on Linux/macOS/Windows: ❌ not run from this local session

## Open questions for the maintainer
- Confirm whether truncation warning records should count against `--max-bytes`; the current implementation always emits the warning as required control metadata after normal record truncation.

## Next recommended milestone
- M2: `axt-fs` and `axt-git` shared crates
